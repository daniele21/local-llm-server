# Public runtime identity API

Status: active
Document type: focused-specification
Owner: runtime identity
Canonical scope: runtime.identity.public-api
Read when: integrating an external evaluator, inspecting resident execution identity, or changing model/runtime/hardware identity semantics
Last reviewed: 2026-08-15

Protocol: `local-llm-identity-v1`  
Endpoint: `GET /v1/runtime/identity`  
Producer task: `LLS-ID-001`

Local LLM Server is the source of truth for the model artifact and serving runtime that are actually resident. The public identity endpoint exposes that information to external evaluation systems such as AI Performance Lab without exposing model paths, download URLs, credentials, prompts, outputs, hostnames or mutable request counters.

The identity API is deliberately separate from `/status`: `/status` is dynamic operational telemetry, while `/v1/runtime/identity` describes the stable execution identity that an evaluator can freeze before a run.

## Responsibility split with AI Performance Lab

The repositories share only the versioned wire contract:

- Local LLM Server owns discovery of resident model artifact identity, backend identity, safe effective runtime configuration and local hardware profile.
- AI Performance Lab owns strict validation, mapping into `ExecutionFingerprint`, comparison semantics, benchmark execution and evidence persistence.
- OpenAI-compatible inference remains independent.
- Dynamic `/status` telemetry remains independent.
- An incompatible schema change requires a new protocol identifier rather than silently mutating `local-llm-identity-v1`.

## Response contract

```json
{
  "protocol_version": "local-llm-identity-v1",
  "server": {
    "name": "local-llm-server",
    "version": "0.4.0"
  },
  "default_model": "nemotron-nano-4b",
  "models": {
    "nemotron-nano-4b": {
      "model": {
        "id": "nvidia/nemotron-3-nano-4b",
        "revision": null,
        "artifact_digest": null,
        "artifact_key": null,
        "quantization": "Q4_K_M",
        "verification": "available_unverified"
      },
      "runtime": {
        "name": "llama_cpp",
        "version": "0.3.x",
        "implementation": "...",
        "config_digest": "<sha256>",
        "config": {
          "backend": "llama_cpp",
          "ctx_size": 36466,
          "n_threads": 8
        },
        "fingerprint": null,
        "captured_at": null,
        "evidence_grade": "partial"
      },
      "hardware": {
        "system": "darwin",
        "machine": "arm64",
        "processor": null,
        "logical_cpus": 10,
        "total_memory_bytes": null,
        "accelerator": null,
        "extra": {}
      }
    }
  }
}
```

`models` is keyed by resident runtime key. `default_model` identifies the current default resident route when one exists.

## Evidence rules

- `model.id` is the effective resident model ID.
- `model.revision` is emitted only when explicitly known.
- `model.artifact_digest` is emitted only from a valid explicit SHA-256 pin and uses the `sha256:<hex>` form.
- `model.artifact_key` is exposed only when the existing artifact-identity/fingerprint path has produced it.
- `model.quantization` is explicit metadata from resolved configuration/registry. It is not inferred from a filename by the public identity API or by consumers.
- `runtime.name` is the effective backend.
- `runtime.version` is resolved from the backend package, an explicit backend version, or a supported backend-specific probe. Unknown stays `null`.
- `runtime.config` contains only the same allowlisted non-sensitive effective settings covered by `runtime.config_digest`.
- `runtime.fingerprint` and `captured_at` are present only when the existing evidence-grade runtime fingerprint was successfully captured.
- `hardware` intentionally excludes hostname or other direct machine identifiers.

`evidence_grade=verified` means the existing runtime fingerprint capture had both a verified artifact SHA-256 and a resolved backend version. A resident runtime may still expose useful partial identity when that stronger evidence is unavailable.

## Effective runtime configuration boundary

`runtime.config` is not an arbitrary dump of the server configuration. It is generated by the same allowlist used for `runtime.config_digest`, including safe execution-relevant values such as backend, context/KV limits, batching/threading, offload/Flash Attention, concurrency and default sampling settings.

Paths, download sources, URLs, credentials and content are excluded. The visible config therefore explains what the digest covers without leaking private serving details.

## Privacy boundary

The serialized contract must not contain:

- `model_path`, `mmproj_path`, local directories or private absolute paths;
- download URLs or repository credentials;
- API keys, authorization headers or environment-variable values;
- prompts, model output, thinking traces or cached inference content;
- hostname or user identity;
- dynamic request counters, phases or throughput samples (those belong to `/status`).

Tests serialize the response and assert that private model paths and download URLs are absent.

## Consumer mapping

AI Performance Lab maps the protocol into its immutable `ExecutionFingerprint`:

- model ID/revision/digest/quantization -> `ModelIdentity`;
- backend name/version/config digest -> `RuntimeIdentity`;
- machine/CPU/accelerator/memory/OS -> `HardwareIdentity`.

The protocol is optional. OpenAI-compatible inference remains independently usable when this endpoint is absent.

## Acceptance contract

`LLS-ID-001` is complete only when:

- the supported product stack exposes public read-only `GET /v1/runtime/identity`;
- the response identifies `local-llm-identity-v1`;
- existing artifact/backend/config/hardware primitives are reused rather than duplicating model-discovery logic;
- quantization is explicit resolved metadata, not consumer filename inference;
- `runtime.config` and `runtime.config_digest` share the same safe allowlist;
- unknown revision/hash/backend-version values remain unknown;
- partial and verified identity are distinguishable;
- private paths, URLs, credentials, content, hostname and dynamic counters cannot leak through the public payload;
- multiple resident runtimes and the default runtime are represented deterministically;
- deterministic tests cover privacy, partial/verified identity and multi-runtime behavior;
- AI Performance Lab consumes the same endpoint/protocol without requiring a Performance Lab dependency in this server.

Passing deterministic CI proves the producer contract implementation. Representative hardware/model runs remain necessary for product evidence about the usefulness and completeness of the observed identity.
