name: Release

on:
  push:
    tags:
      - "v*.*.*"   # trigger on version tags, e.g. v0.2.0

permissions:
  contents: write   # needed to create GitHub Releases

jobs:
  build:
    name: Build distribution
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install build frontend
        run: pip install build
      - name: Build wheel and sdist
        run: python -m build --outdir dist/
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/

  github-release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/
      - name: Create release and attach assets
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*
          generate_release_notes: true

  # pypi-publish:
  #   name: Publish to PyPI
  #   needs: build
  #   runs-on: ubuntu-latest
  #   environment: pypi          # create this environment in repo Settings → Environments
  #   permissions:
  #     id-token: write          # required for Trusted Publishing (no API token needed)
  #   steps:
  #     - uses: actions/download-artifact@v4
  #       with:
  #         name: dist
  #         path: dist/
  #     - name: Publish to PyPI
  #       uses: pypa/gh-action-pypi-publish@release/v1
  #       # Uses PyPI Trusted Publishing — no PYPI_API_TOKEN secret required.
  #       # Configure it at: https://pypi.org/manage/account/publishing/
