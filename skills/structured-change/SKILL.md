---
name: structured-change
description: Guard meaningful Local LLM Server changes against duplicate ownership, unresolved assumptions, unsafe lifecycle/privacy/evidence behavior and unnecessary complexity without making every edit publication-ready.
---

# Structured Change

Find one canonical owner and inspect direct consumers/fakes/tests before shared-contract edits. Preserve task/capability fail-closed semantics, path-free runtime identity, evidence provenance, resource ownership, bounded concurrency, cleanup and local-first privacy.

Prefer one observable vertical outcome; technical layers are subtasks unless independently useful. Spend abstractions/dependencies/workers/UI patterns only for a concrete need. Treat failure, cancellation, shutdown, restart and partial initialization as normal behavior.

Read `.engineering/commands.json` for build/runtime/validation changes and `.engineering/e2e.json` when a complete workflow or environment claim changes. Never promote deterministic Linux/fixture evidence into Apple Silicon/model/memory/performance evidence. For product UI, resolve task/journey/hierarchy/states/accessibility before polish and reuse canonical design owners.

During `ITERATION`, keep validation focused and documentation/publication ceremony proportional. Exact-head, complete-diff and durable-documentation readiness begin when the coherent outcome moves to `INTEGRATION` or `RELEASE` through `preflight-change`. Automatable deterministic gates remain repository-owned even when the current agent lacks the toolchain.
